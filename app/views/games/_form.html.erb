<%= form_with(model: game) do |form| %>
  <% if game.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
        <% game.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3" style="width: 350px;">
    <%= form.label :name, "#{ t '.name' }", style: "display: block" %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="mb-3" style="width: 350px;">
    <%= form.label :image, "#{ t '.pic' }", style: "display: block;" %>
    <% if game.image.attached? %>
      <%= image_tag game.image, style: 'width: 200px; display: block;' %> <br>
    <% end %>
    <%= form.file_field :image, class: 'form-control', accept: 'image/jpg, image/png, image/jpeg' %>
    <div class="form-text" style="width: 300px"><%= t '.comm' %></div>
  </div>

  <div>
    <% if action_name == 'new' %>
      <%= form.submit "#{ t '.add' }", class:'btn btn-success' %>
    <% else %>
      <%= form.submit "#{ t '.edit' }", class:'btn btn-success' %>
    <% end %>
  </div>
<% end %>
